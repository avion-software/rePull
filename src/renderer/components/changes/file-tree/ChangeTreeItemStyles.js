import { makeStyles } from '@material-ui/core';

const useChangeTreeItemStyles = makeStyles(() => ({
    root: {
    },
    rootSelected: {
        backgroundColor: 'rgba(255, 255, 255, 0.3)',
    },
}));

export default useChangeTreeItemStyles;
