import { createMuiTheme } from '@material-ui/core';

const theme = createMuiTheme({
    palette: {
        type: 'dark',
        primary: {
            main: '#0d47a1',
        },
        secondary: {
            main: '#311b92',
        },
    },
});

export default theme;
